[phases.setup]
# "..." preserves the provider defaults (Python + pip); graphviz adds the system binary
nixPkgs = ["...", "graphviz"]

[phases.build]
cmds = ["pip install -e ."]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
